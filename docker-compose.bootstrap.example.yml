services:
  bootstrap:
    build:
      context: .
      dockerfile: Dockerfile.bootstrap
      args:
        VERSION: ${MURMUR_VERSION:-0.0.0-dev}
        COMMIT: ${MURMUR_COMMIT:-unknown}
    image: murmur-bootstrap:${MURMUR_VERSION:-dev}
    container_name: murmur-bootstrap
    restart: unless-stopped
    ports:
      - "${BOOTSTRAP_PORT:-8081}:8081"
      - "${BOOTSTRAP_P2P_TCP_PORT:-4001}:4001"
      - "${BOOTSTRAP_P2P_UDP_PORT:-4001}:4001/udp"
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN:-}
      NGROK_DOMAIN: ${NGROK_DOMAIN:-}
      ALLOW_ORIGIN: ${ALLOW_ORIGIN:-*}
      ANNOUNCE_ADDRS: ${ANNOUNCE_ADDRS:-}
    volumes:
      - ./bootstrap-data:/data
    command:
      - /bin/sh
      - -ec
      - |
        set -eu
        args="-state-dir=/data -listen=:8081 -p2p-listen=/ip4/0.0.0.0/tcp/4001,/ip4/0.0.0.0/udp/4001/quic-v1 -allow-origin=$${ALLOW_ORIGIN}"
        if [ -n "$${ANNOUNCE_ADDRS}" ]; then
          args="$${args} -announce-addrs=$${ANNOUNCE_ADDRS}"
        fi
        if [ -n "$${NGROK_AUTHTOKEN}" ]; then
          args="$${args} -ngrok"
        fi
        if [ -n "$${NGROK_DOMAIN}" ]; then
          args="$${args} -ngrok-domain=$${NGROK_DOMAIN}"
        fi
        exec /usr/local/bin/murmur-bootstrap $${args}

# Example usage:
#   mkdir -p bootstrap-data
#   docker compose -f docker-compose.bootstrap.example.yml up --build -d
#
# Optional public multiaddr advertisement:
#   export ANNOUNCE_ADDRS=/dns4/bootstrap.example.org/tcp/4001,/dns4/bootstrap.example.org/udp/4001/quic-v1
#
# Optional ngrok configuration:
#   export NGROK_AUTHTOKEN=...
#   export NGROK_DOMAIN=consuming-dangling-commodore.ngrok-free.dev